The plotting script ~plot.sh~ generates simulation diagrams in PDF format.

+ ~plot.sh~ must be invoked from within the ~plot/~ directory
+ =./plot.sh example/example.dot= plots the diagram from a single ~.dot~ file
+ =./plot.sh= (with no file supplied) plots all ~.dot~ files in the ~plot/~ directory
